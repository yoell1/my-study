# Java의 특징과 장단점

> 2026-09-22 · Java 기초

## 1. Java란?

1995년 썬 마이크로시스템즈(현재 오라클)에서 발표한 **객체 지향 프로그래밍 언어**.
"한 번 작성하면 어디서나 실행된다(Write Once, Run Anywhere)"가 대표 슬로건이다.

---

## 2. Java의 실행 과정

```
[작성]            [컴파일]              [실행]
Hello.java  →  javac  →  Hello.class  →  JVM  →  Windows / macOS / Linux
(소스 코드)             (바이트코드)       (운영체제별 JVM이 해석·실행)
```

- `javac` 가 소스 코드를 **바이트코드(.class)** 로 바꾼다 — 기계어가 아니다
- 바이트코드는 **JVM(Java Virtual Machine)** 이 읽어서 실행한다
- 운영체제마다 그에 맞는 JVM이 있으므로, 같은 `.class` 파일이 어디서든 돈다

| 용어 | 뜻 |
| --- | --- |
| JDK | Java Development Kit — 개발 도구 모음 (컴파일러 `javac` + JRE) |
| JRE | Java Runtime Environment — 실행 환경 (JVM + 기본 라이브러리) |
| JVM | Java Virtual Machine — 바이트코드를 실제로 실행하는 가상 기계 |

---

## 3. Java의 특징

### 3-1. 플랫폼 독립성 (이식성이 높다)

프로그램이 운영체제 위에서 바로 돌지 않고 **JVM이 중간에 끼어 있기** 때문에,
한 번 컴파일한 파일을 운영체제가 달라도 그대로 실행할 수 있다.
(JVM 자체는 운영체제마다 따로 설치해야 한다 — 즉 JVM은 플랫폼에 종속적이다.)

### 3-2. 객체 지향 언어

프로그램을 명령어를 순서대로 나열한 것으로 보지 않고,
**서로 협력하는 여러 개의 독립된 "객체"의 모임**으로 본다.

객체 지향의 4가지 특징
- **캡슐화** — 데이터와 기능을 하나로 묶고, 외부에서 함부로 못 건드리게 숨긴다 (`private`)
- **상속** — 기존 클래스를 물려받아 새 클래스를 만든다 (`extends`)
- **다형성** — 같은 이름의 메서드가 객체에 따라 다르게 동작한다 (오버라이딩)
- **추상화** — 공통된 특징만 뽑아서 설계한다 (추상 클래스, 인터페이스)

### 3-3. 자동 메모리 관리 (Garbage Collection)

C/C++은 개발자가 메모리를 직접 해제해야 하지만,
Java는 **GC(가비지 컬렉터)** 가 더 이상 쓰지 않는 객체를 알아서 정리한다.

### 3-4. 컴파일 + 인터프리트를 함께 쓰는 언어

- 컴파일 언어: 코드 전체를 먼저 번역한 뒤 실행 (C, C++)
- 인터프리터 언어: 한 줄씩 번역하면서 실행 (Python, JavaScript)
- Java: 소스를 바이트코드로 **컴파일**한 뒤, JVM이 바이트코드를 **해석·실행**
  (자주 실행되는 부분은 JIT 컴파일러가 기계어로 바꿔 속도를 높인다)

### 3-5. 정적 타입 언어

변수를 만들 때 **자료형을 반드시 정해야** 한다.

```java
int age = 20;          // 정수만 담을 수 있음
String name = "우진";   // 문자열만 담을 수 있음
// age = "스무살";      // 컴파일 에러 — 실행 전에 실수를 잡아준다
```

파이썬(`age = 20` 후 `age = "스무살"` 가능)과 가장 크게 다른 점.

### 3-6. 동적 로딩 지원

프로그램 시작 시 모든 클래스를 한 번에 올리지 않고,
**실행 중 필요한 시점에** 클래스를 메모리에 불러온다.

### 3-7. 멀티스레드 지원

하나의 프로그램 안에서 여러 작업(스레드)을 동시에 실행할 수 있고,
이를 위한 기능(`Thread`, `synchronized` 등)을 언어 차원에서 제공한다.

### 3-8. 풍부한 표준 라이브러리와 생태계

컬렉션, 입출력, 네트워크 등 기본 라이브러리가 풍부하고,
Spring 같은 프레임워크 덕분에 기업용 서버 개발에 많이 쓰인다.

---

## 4. 장점과 단점

### 장점

| 장점 | 이유 |
| --- | --- |
| 운영체제에 상관없이 실행 | JVM이 중간에서 실행해준다 |
| 메모리 관리 부담이 적다 | GC가 자동으로 정리 |
| 실수를 일찍 발견 | 정적 타입 — 자료형 오류를 컴파일 단계에서 잡음 |
| 안정성 | 포인터를 직접 다루지 않고, 예외 처리 구조가 탄탄함 |
| 유지보수·협업에 유리 | 객체 지향 구조 + 명확한 타입 |
| 자료와 생태계가 풍부 | 오래 쓰인 언어라 라이브러리·레퍼런스가 많음 |

### 단점

| 단점 | 이유 |
| --- | --- |
| 실행 속도가 상대적으로 느림 | JVM을 거쳐야 함 (JIT 덕분에 차이는 많이 줄었음) |
| 코드가 길다 | 타입 선언, 클래스 구조 등 써야 할 코드가 많음 |
| 메모리를 많이 씀 | JVM 자체가 메모리를 차지 |
| GC 멈춤 | GC가 도는 순간 프로그램이 잠깐 멈출 수 있음 |
| 예외 처리가 번거로움 | 체크 예외는 반드시 `try-catch` 나 `throws` 로 처리해야 함 |

---

## 5. 한 줄 정리

> Java는 **JVM 위에서 돌아가는 객체 지향 언어**다.
> 어디서나 실행되고 안정적인 대신, 코드가 길고 C/C++보다 무겁다.
